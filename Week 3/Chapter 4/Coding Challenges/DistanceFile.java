import java.util.Scanner;

public class DistanceFile {
    public static void main(String[] args) {
        Scanner kb = new Scanner(System.in);

        // Write report for DistanceTraveled.java onto a file (Notepad) instead of the terminal


        kb.close();
    }
}
